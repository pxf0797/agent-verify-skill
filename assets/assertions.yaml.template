# ============================================================
# agent-verify Assertions — 项目: {{TARGET_NAME}}
# ============================================================
# 用自然语言添加断言: agent-verify add "你的规则"
# 断言类型: tool_call | tool_param | output_match
# ============================================================

assertions:
- id: example-tool-call
  name: '示例: 必须调用某工具'
  type: tool_call
  target:
    tool_name: web_search
  condition:
    min_count: 1
  severity: error
  enabled: false

